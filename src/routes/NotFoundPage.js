import { Grid, Typography } from "@mui/material";
import React from "react";

const NotFoundPage = () => {
  return (
    <Grid>
      <Typography>404</Typography>
      <Typography>Not found! Sorry!</Typography>
    </Grid>
  );
};

export default NotFoundPage;
