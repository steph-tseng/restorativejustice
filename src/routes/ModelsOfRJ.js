import { Button, Grid, Typography } from "@mui/material";
import React from "react";

const ModelsOfRJ = () => {
  return (
    <Grid>
      <Button>
        <Typography>Conferencing Circle</Typography>
      </Button>
    </Grid>
  );
};

export default ModelsOfRJ;
